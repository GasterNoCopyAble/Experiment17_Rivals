name: Experiment17 Module Test

on:
  push:
    paths:
      - '.github/workflows/module-test.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "Experiment17 runner works"
